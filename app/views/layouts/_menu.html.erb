<nav>
  <% if logged_in? %>
      <%= t('user.greating', name: current_user.name) %>
    <% else %>
      <ul>
        <li><%=link_to t('menu.signin'), login_path %></li>
        <li><%=link_to t('menu.signup'), signup_path %></li>
      </ul>
  <% end %>
</nav>
